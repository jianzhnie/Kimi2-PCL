
# 本周工作

## 已完成

### VLLM:
1. 迁移qwen3的attention类到deepseekV2 中，将结构修改为GQA。
2. 当前训练权重和qwen3权重也不完全对齐，当前训练权重是up_proj、gate_proj、down_proj;qwen3中key值gate_proj包含gate_proj与down_proj，当前手动构造_pack_weights方法，将两种key值拼接。
3. 发现代码中仍存在kv_a_layernorm等MLA特征的内容，正在排查修改中。

### MINDSPEED:
1. 使用9600步ckpt重新完成权重转换并开展推理验证，模型输出仍为乱码，无法正常完成评测回答。
2. 开展对照验证，使用Pytorch直接加载mcore2hf脚本转换后的权重，可实现正常推理，定界到hf2mcore脚本。
3. 对比权重数值，发现stage0的MLP向上投影矩阵权重数值存在不一致，后续重点分析脚本中MLP fc1权重转换相应代码。
4. 检查和优化转换脚本，修复转换过程存在的bug。

## 正在进行中
1. 基于最新的脚本，正在检查转换回去的 Mcore 模型参数是否与训练的 Ckpt 模型参数一致。
2. 转换最新的ckpt，使用 MingSpeed 模型加载，进行推理验证，确认模型输出是否正常。
3. vllm 推理持续适配
